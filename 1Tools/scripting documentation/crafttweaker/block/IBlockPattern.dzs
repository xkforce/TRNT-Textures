import crafttweaker.block.IBlock;

zenClass IBlockPattern {
    val displayName as string;
    
    function blocks() as [IBlock];
    
    operator |(pattern as IBlockPattern) as IBlockPattern;
    
    operator has(block as IBlock) as bool;
}
